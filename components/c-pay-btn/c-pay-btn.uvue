<script setup>
	/**
	 * 支付按钮组件
	 */
	import type { DeviceInfo } from '@/api/types/deviceInfoTypes.uts'
	const props = defineProps({
		list: {
			type: Array as PropType<DeviceInfo[]>,
			default: () => []
		}
	})
	const emits = defineEmits<{
		(e : "onTap", event : DeviceInfo) : void
	}>()
	
	function onTapFn(e : DeviceInfo) {
		emits('onTap', e)
	}
</script>

<template>
	<view class="payBtnBox">
		<view v-for="(item,index) in list" hover-class="hover-class" :hover-stay-time="200"
			:class="['payBtnBox-item',{'payBtnBox-item-first':index==0}]" :style="{backgroundColor:item.backgorundColor}"
			@click="onTapFn(item)">
			<image class="payBtnBox-item-image" :src="item.image"></image>
			<view class="textBox">
				<text :style="{color:item.color}" class="text-title">{{item.title}}</text>
				<text :style="{color:item.color}" class="text-subTitle">{{item.sub_title}}</text>
				<text :style="{color:item.color}" class="text-desc">{{item.desc}}</text>
			</view>
		</view>
	</view>
</template>

<style lang="scss">
	.hover-class {
		transform: translate(1rpx, 1rpx);
	}

	.payBtnBox {
		align-items: center;
		flex-direction: row;
		justify-content: space-between;
		width: 100%;
		height: 100%;

		.payBtnBox-item-first {
			margin-left: 0 !important;
		}

		.payBtnBox-item {
			flex: 1;
			// width: 352px;
			height: 100%;
			display: flex;
			flex-direction: row;
			align-items:flex-start;
			border-radius: 16rpx;
			padding: 10rpx 12rpx;
			margin-left: 9rpx;
			.textBox {
				position: relative;
				display: flex;
				flex: 1;
				flex-direction: column;
				align-items: flex-start;
				.text-title {
					text-align: left;
					font-size: 13rpx;
					font-weight: bold;
				}

				.text-subTitle {
					margin: 3rpx 0;
					font-size: 9rpx;
					text-align: left;
				}

				.text-desc {
					height: 19rpx;
					border-radius: 4rpx;
					background-color: #fff;
					padding: 0 4rpx;
					text-align: left;
					line-height: 20rpx;
					font-size: 8rpx;
				}
			}

			.payBtnBox-item-image {
				position: absolute;
				z-index: -1;
				right: 8rpx;
				bottom: 10rpx;
				width: 44rpx;
				height: 44rpx;
			}
		}
	}
</style>