<template>
	<view class="tempBox" >
		<view class="tempBox-item">
			<image class="tempBox-item-icon" src="/static/icon/room.png" />
			<view class="tempBox-item-box">
				<text class="tempBox-item-text"> 室温： </text>
				<text class="tempBox-item-text-bold">{{ cage}}</text>
				<text class="tempBox-item-text"> ℃ </text>
			</view>
		</view>
		<view class="line"/>
		<view class="tempBox-item">
			<image class="tempBox-item-icon" src="/static/icon/water.png" />
			<view class="tempBox-item-box">
				<text class="tempBox-item-text"> 水温：</text>
				<text class="tempBox-item-text-bold">{{ water }}</text>
				<text class="tempBox-item-text"> ℃ </text>
			</view>
		</view>
	</view>
</template>

<script setup>
const props = defineProps({
  cage: {
    type: [Number, String],
    default: 0
  },
  water: {
    type: [Number, String],
    default: 0
  }
})
</script>

<style lang="scss">
.tempBox {
	position: fixed;
	right: 15rpx;  /* 50px */
	top: 9rpx;     /* 30px */
	z-index: 25;
	display: flex;
	flex-direction: row;
	align-items: center;
	transform-origin: right top;
	background-color: #c3e9fe;
	width: 118rpx;   /* 404px */
	height: 16rpx;   /* 53px */
	border-radius: 10rpx;  /* 33px */

	.line {
		height: 6rpx;   /* 20px */
		width: 1rpx;    /* 2px */
		transform: scaleX(0.5);
		background-color: #236E99;
	}

	.tempBox-item {
		flex: 1;
		display: flex;
		align-items: center;
		flex-direction: row;
		justify-content: flex-start;
		padding-left: 6rpx;   /* 20px */

		.tempBox-item-box {
			display: flex;
			flex-direction: row;
			align-items: center;
		}

		.tempBox-item-icon {
			width: 7rpx;   /* 25px */
			height: 7rpx;  /* 25px */
			margin-right: 2rpx;  /* 7px */
		}

		.tempBox-item-text {
			color: #2E87B5;
			font-size: 6rpx;  /* 22px */
		}
		.tempBox-item-text-bold {
			font-weight: bold;
			color: #2E87B5;
			font-size: 6rpx;  /* 22px */
		}
	}
}

</style>