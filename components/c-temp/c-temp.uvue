<template>
	<view class="tempBox">
		<view class="tempBox-item">
			<image class="tempBox-item-icon" src="/static/icon/room.png" />
			<view class="tempBox-item-box">
				<text class="tempBox-item-text-bold"> 室温： </text>
				<text class="tempBox-item-text-bold">{{ cage }}</text>
				<text class="tempBox-item-text-bold"> ℃ </text>
			</view>
		</view>
		<view class="line" />
		<view class="tempBox-item">
			<image class="tempBox-item-icon" src="/static/icon/water.png" />
			<view class="tempBox-item-box">
				<text class="tempBox-item-text-bold"> 水温：</text>
				<text class="tempBox-item-text-bold">{{ water }}</text>
				<text class="tempBox-item-text-bold"> ℃ </text>
			</view>
		</view>
	</view>
</template>

<script setup>
	/**
	 * 右上角温度显示
	 */
	const props = defineProps({
		cage: {
			type: [Number, String],
			default: 0
		},
		water: {
			type: [Number, String],
			default: 0
		}
	})
</script>

<style lang="scss">
	.tempBox {
		position: fixed;
		right: 15rpx;
		/* 50px */
		top: 9rpx;
		/* 30px */
		z-index: 25;
		display: flex;
		flex-direction: row;
		align-items: center;
		transform-origin: right top;
		background-color: #c3e9fe;
		width: 118rpx;
		/* 404px */
		height: 16rpx;
		/* 53px */
		border-radius: 10rpx;
		/* 33px */

		box-shadow: 1rpx 1rpx 4rpx 2rpx rgba(100, 100, 100, 0.15);


		.line {
			height: 6rpx;
			/* 20px */
			width: 2rpx;
			/* 2px */
			transform: scaleX(0.4);
			background-color: rgba(35, 110, 153, 0.5);
		}

		.tempBox-item {
			flex: 1;
			display: flex;
			align-items: center;
			flex-direction: row;
			justify-content: flex-start;
			padding-left: 6rpx;
			/* 20px */

			.tempBox-item-box {
				display: flex;
				flex-direction: row;
				align-items: center;
			}

			.tempBox-item-icon {
				width: 7rpx;
				/* 25px */
				height: 7rpx;
				/* 25px */
				margin-right: 2rpx;
				/* 7px */
			}

			.tempBox-item-text {
				color: #2E87B5;
				font-size: 6rpx;
				/* 22px */
				text-align: center;
			}

			.tempBox-item-text-bold {
				font-weight: bold;
				color: #2E87B5;
				font-size: 6rpx;
				/* 22px */
				text-align: center;
			}
		}
	}
</style>