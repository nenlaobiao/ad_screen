<script setup>
	/**
	 * 右上角温度显示
	 */
	const props = defineProps({
		cage: {
			type: [Number, String],
			default: 10.1
		},
		water: {
			type: [Number, String],
			default: 20.2
		}
	})
</script>

<template>
	<view class="tempBox">
		<image class="tempBox-item-icon" src="/static/icon/room.png" />
		<text class="tempBox-item-text">室温：{{ cage }} ℃</text>
		<view class="line" />
		<image class="tempBox-item-icon" src="/static/icon/water.png" />
		<text class="tempBox-item-text">水温：{{ water }} ℃</text>
	</view>
</template>

<style lang="scss">
	.tempBox {
		position: fixed;
		right: 20rpx;
		top: 7rpx;
		z-index: 25;
		display: flex;
		flex-direction: row;
		align-items: center;
		background-color: #c3e9fe;
		min-width: 120rpx;
		height: 16rpx;
		border-radius: 10rpx;
		box-shadow: 1rpx 1rpx 4rpx 2rpx rgba(100, 100, 100, 0.15);
		padding: 0 6rpx;

		.line {
			height: 6rpx;
			width: 2rpx;
			transform: scaleX(0.4);
			background-color: rgba(35, 110, 153, 0.5);
			margin: 0 6rpx;
		}
		
		.tempBox-item-icon {
			width: 7rpx;
			height: 7rpx;
			margin-right: 3rpx;
		}

		.tempBox-item-text {
			flex:1;
			margin-top: 1rpx;
			// font-weight: bold;
			color: #2E87B5;
			font-size: 7rpx;
		}
	}
</style>