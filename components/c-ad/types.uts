export type AdFit = "contain" | "fill" | "cover"

export type State = {
	videoId:string;
	current:number;
	length:number;
	isPlay:boolean;
	timer:null|number;
}
export type CAdChange = {
	id: number;
	index:number;
	type: "touch" | "autoplay"
	isPlay: boolean
}
