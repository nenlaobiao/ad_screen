import type { dFile } from '@/pages/index/types.uts'
export type AdFit = "contain" | "fill" | "cover"
export type State = {
	list:dFile[];
	videoId:string;
	current:number;
	length:number;
	isPlay:boolean;
	timer:null|number;
	inRepair:boolean;
	repairList:dFile[];
}
export type CAdChange = {
	id: number;
	index:number;
	type: "touch" | "autoplay"
	isPlay: boolean
}
